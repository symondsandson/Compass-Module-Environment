db:
  image: orchardup/postgresql
  ports:
    - "5432"

compass:
  build: ./Compass
  command: bundle exec rackup -p 3000
  volumes:
    - ./Compass:/app
  ports:
    - "3000:3000"
  links:
    - db
