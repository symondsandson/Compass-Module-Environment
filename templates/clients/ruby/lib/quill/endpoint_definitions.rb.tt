module Quill
  module EndpointDefinitions
  <% Quill::API.endpoints.each do |endpoint, _| %>
    def <%= endpoint %>
      Quill::Endpoints::<%= endpoint.camelize %>.new(self)
    end
  <% end %>
  end
end
